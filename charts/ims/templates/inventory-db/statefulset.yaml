apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-inventory-db
  labels:
    {{- include "ims.componentLabels" (dict "context" $ "component" "inventory-db") | nindent 4 }}
spec:
  serviceName: {{ .Release.Name }}-inventory-db
  replicas: 1
  selector:
    matchLabels:
      {{- include "ims.selectorLabels" (dict "context" $ "component" "inventory-db") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ims.componentLabels" (dict "context" $ "component" "inventory-db") | nindent 8 }}
    spec:
      containers:
        - name: postgres
          image: "{{ .Values.inventoryDb.image.repository }}:{{ .Values.inventoryDb.image.tag }}"
          imagePullPolicy: {{ .Values.inventoryDb.image.pullPolicy }}
          env:
            - name: POSTGRES_DB
              value: {{ .Values.inventoryDb.database | quote }}
            - name: POSTGRES_USER
              value: {{ .Values.inventoryDb.username | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.inventoryDb.password | quote }}
          ports:
            - containerPort: {{ .Values.inventoryDb.port }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.inventoryDb.username | quote }}, "-d", {{ .Values.inventoryDb.database | quote }}]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", {{ .Values.inventoryDb.username | quote }}, "-d", {{ .Values.inventoryDb.database | quote }}]
            initialDelaySeconds: 10
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.inventoryDb.storage.size }}
