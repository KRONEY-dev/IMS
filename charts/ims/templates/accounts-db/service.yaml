apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-accounts-db
  labels:
    {{- include "ims.componentLabels" (dict "context" $ "component" "accounts-db") | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "ims.selectorLabels" (dict "context" $ "component" "accounts-db") | nindent 4 }}
  ports:
    - port: {{ .Values.accountsDb.port }}
      targetPort: {{ .Values.accountsDb.port }}
